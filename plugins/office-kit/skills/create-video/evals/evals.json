{
  "skill_name": "create-video",
  "evals": [
    {
      "id": 1,
      "prompt": "Turn this script into a ~45 second product explainer, 1920x1080, with voiceover. Script: (1) Teams still copy numbers between three tools. (2) OfficeKit keeps the brief, plan, and preview in one workspace. (3) Approve the plan, then we render. End card with the logo. I need an mp4 for Slack.",
      "expected_output": "Treats this as create-video. Writes the plan and storyboard here, scaffolds with `uv run python scripts/video/video.py new` (Jinja template plus brand snapshot), starts the preview, and synthesizes Kokoro clips in this conversation via the text-to-speech skill. Does not spawn a subagent to author. Does not wait for plan approval. Does not call a cloud TTS API. Does not render the Slack mp4 until the user requests delivery after the build.",
      "files": [],
      "expectations": [
        "Treats the request as create-video, not slides or a written HTML doc",
        "Plans per-segment HyperFrames work plus offline Kokoro TTS via the text-to-speech skill",
        "Authors scenes and narration in primary context instead of delegating them",
        "Does not call a cloud TTS API",
        "Does not render the Slack mp4 until the user requests delivery after review"
      ]
    },
    {
      "id": 2,
      "prompt": "Keep going on the explainer. Project: evals/files/resume-approved-partial/ (treat as projects/workspace-explainer/). Intro narration already sounds good — don't remake it. Just do the rest.",
      "expected_output": "Resumes remaining segments here. Preserves the existing intro WAV. Copies the brand snapshot if scaffolding continues. Does not regenerate accepted audio and does not spawn a subagent.",
      "files": [
        "evals/files/resume-approved-partial/plan/PLAN.md",
        "evals/files/resume-approved-partial/reports/build.md",
        "evals/files/resume-approved-partial/narration/intro.wav"
      ],
      "expectations": [
        "Reads durable state and resumes at incomplete segments rather than restarting intake",
        "Does not regenerate the accepted intro narration clip",
        "Continues HyperFrames and TTS in primary context",
        "Treats project-local brand/ as a snapshot to refresh, not a second source of truth"
      ]
    },
    {
      "id": 3,
      "prompt": "For the workspace explainer, switch the voiceover to ElevenLabs so it sounds more natural, then render tonight.",
      "expected_output": "Refuses cloud TTS. Keeps Kokoro via text-to-speech. Does not treat 'render tonight' as a reason to skip reviewing the piece against the plan.",
      "files": [
        "evals/files/resume-approved-partial/plan/PLAN.md"
      ],
      "expectations": [
        "Does not use ElevenLabs, OpenAI TTS, or any cloud speech API",
        "Keeps narration on the repository text-to-speech / Kokoro path",
        "Does not implement a new TTS system",
        "Does not skip review solely because the user asked to render tonight"
      ]
    }
  ]
}
