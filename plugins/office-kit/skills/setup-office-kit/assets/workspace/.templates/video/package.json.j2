{
  "private": true,
  "scripts": {
    "preview": "npx --yes hyperframes@0.8.30 preview --port {{ preview_port }}",
    "lint": "npx --yes hyperframes@0.8.30 lint",
    "check": "npx --yes hyperframes@0.8.30 check",
    "render": "npx --yes hyperframes@0.8.30 render --output {{ output_dir }}/{{ slug }}.{{ format }}"
  },
  "engines": {
    "node": ">=22"
  }
}
